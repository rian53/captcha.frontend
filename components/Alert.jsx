import { useState, useEffect } from 'react';
import { useRouter } from 'next/router';

function Alert({ id = "default-alert", fade = true }) {
  // Componente simplificado - não renderiza nada
  // O sistema de alertas agora usa toast do sonner
  return null;
}

export {Alert}
